{
  "name": "mock-server",
  "version": "1.0.0",
  "description": "Mock API for MyBanks",
  "scripts": {
    "start": "json-server --watch db.json --routes routes.json --port 3001 --host 0.0.0.0 "
  },
  "dependencies": {
    "json-server": "^0.17.0"
  },
  "devDependencies": {
    "@nuxt/schema": "^3.16.1"
  }
}
